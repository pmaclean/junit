package example1;

public class DuckWeighIn1 {

	// Find the weights for all ducks
	public Float Sum(Float[] duckWeights)
	{
		Float accumulator = 0f;
		for (int i=0; i<duckWeights.length; i++)
		{
			accumulator += duckWeights[i];
		}
		return accumulator;
	}
}
